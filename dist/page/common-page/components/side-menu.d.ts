/// <reference types="react" />
import { MenuItem } from "../../../types/card";
declare type Props = {
    isExpanded: boolean;
    menuContents: MenuItem[];
    className?: string;
};
export declare const SideMenu: (props: Props) => JSX.Element;
export {};
