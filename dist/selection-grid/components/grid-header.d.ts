import React, { ReactNode } from "react";
import { SelectionGridClasses } from "../../types/selection-grid";
declare type Props = {
    children?: ReactNode;
    classes?: SelectionGridClasses;
};
export declare const GridHeader: React.FC<Props>;
export {};
